name: Pipeline CI/CD - Homologação (Otimizado v2)
run-name: Pipeline CI executado por ${{ github.actor }}

on:
  workflow_dispatch:
    inputs:
      version-override:
        description: 'Sobrescrever versão do package.json (opcional, ex: 1.0.0)'
        required: false
        type: string
      create-tag:
        description: 'Criar tag Git com a versão?'
        required: false
        type: boolean
        default: false
      bump-type:
        description: 'Tipo de incremento de versão (se não informar version-override)'
        required: false
        type: choice
        options:
          - none
          - patch
          - minor
          - major
        default: none

permissions:
  contents: write
  security-events: write

jobs:
  ambiente-para-homologacao:
    name: Verificação dos Ambientes
    strategy:
      matrix:
        versao: [20, 22]
        plataforma: ["ubuntu"]
    runs-on: ${{ matrix.plataforma }}-latest
    
    steps:
      - name: Instalação do Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.versao }}
      
      - name: Exibição da versão e da plataforma
        run: |
          node --version
          echo ${{ runner.os }}

  build:
    name: Build
    needs: [ambiente-para-homologacao]
    runs-on: ubuntu-latest
    environment: homologacao
    outputs:
      version: ${{ steps.version.outputs.version }}
    
    steps:
      - name: Checkout do código
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
      
      - name: Ativar pnpm
        run: corepack enable
      
      - name: Cache de dependências
        uses: actions/cache@v4
        with:
          path: |
            ./api/node_modules
            ~/.pnpm-store
          key: ${{ runner.os }}-pnpm-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-
      
      - name: Limpar instalação anterior
        working-directory: ./api
        run: |
          rm -rf node_modules .turbo dist
      
      - name: Instalar dependências
        working-directory: ./api
        run: pnpm install --frozen-lockfile
      
      - name: Determinar versão
        id: version
        working-directory: ./api
        run: |
          bump_version() {
            local version=$1
            local bump_type=$2
            
            IFS='.' read -ra VERSION_PARTS <<< "$version"
            MAJOR="${VERSION_PARTS[0]}"
            MINOR="${VERSION_PARTS[1]}"
            PATCH="${VERSION_PARTS[2]}"
            
            case $bump_type in
              major)
                MAJOR=$((MAJOR + 1))
                MINOR=0
                PATCH=0
                ;;
              minor)
                MINOR=$((MINOR + 1))
                PATCH=0
                ;;
              patch)
                PATCH=$((PATCH + 1))
                ;;
            esac
            
            echo "$MAJOR.$MINOR.$PATCH"
          }
          
          PACKAGE_VERSION=$(node -p "require('./package.json').version")
          echo "Versão do package.json: $PACKAGE_VERSION"
          
          if [ -n "${{ inputs.version-override }}" ]; then
            VERSION="${{ inputs.version-override }}"
            echo "Usando versão manual: $VERSION"
          elif [ "${{ inputs.bump-type }}" != "none" ]; then
            VERSION=$(bump_version "$PACKAGE_VERSION" "${{ inputs.bump-type }}")
            echo "Versão incrementada (${{ inputs.bump-type }}): $VERSION"
          else
            VERSION="$PACKAGE_VERSION"
            echo "Usando versão do package.json: $VERSION"
          fi
          
          if ! [[ "$VERSION" =~ ^[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
            echo "Versão inválida: $VERSION"
            echo "Formato esperado: X.Y.Z (ex: 1.0.0, 2.3.4)"
            exit 1
          fi
          
          echo "Versão final: $VERSION"
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "VERSION=$VERSION" >> $GITHUB_ENV
      
      - name: Atualizar package.json
        if: ${{ inputs.version-override != '' || inputs.bump-type != 'none' }}
        working-directory: ./api
        run: |
          echo "Atualizando package.json para versão ${{ steps.version.outputs.version }}"
          npm version ${{ steps.version.outputs.version }} --no-git-tag-version --allow-same-version
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          git add package.json
          git commit -m "chore: bump version to ${{ steps.version.outputs.version }} [skip ci]" || echo "Nenhuma alteração"
      
      - name: Gerar Prisma Client (sem banco)
        working-directory: ./api
        env:
          DATABASE_URL: postgresql://user:pass@localhost:5432/db?schema=public
        run: pnpm run generate
      
      - name: Verificar TypeScript
        continue-on-error: true
        working-directory: ./api
        run: |
          echo "Verificando versão do TypeScript:"
          pnpm tsc --version
          echo "Executando type check:"
          pnpm tsc --noEmit --pretty
      
      - name: Build do projeto
        working-directory: ./api
        run: pnpm build
      
      - name: Verificar build gerado
        working-directory: ./api
        run: |
          echo "Conteúdo do diretório dist:"
          ls -lah dist/
          test -f dist/server.js && echo "[SUCESSO] server.js encontrado" || (echo "[ERROR] server.js NÃO encontrado" && exit 1)
      
      - name: Upload do build
        uses: actions/upload-artifact@v4
        with:
          name: build-artifacts
          path: |
            ./api/dist
            ./api/node_modules
          retention-days: 1

  testes-unitarios:
    name: Testes Unitários
    needs: [build]
    runs-on: ubuntu-latest
    environment: homologacao
    
    steps:
      - name: Checkout do código
        uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
      
      - name: Ativar pnpm
        run: corepack enable
      
      - name: Cache de dependências
        uses: actions/cache@v4
        with:
          path: |
            ./api/node_modules
            ~/.pnpm-store
          key: ${{ runner.os }}-pnpm-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-
      
      - name: Instalar dependências
        working-directory: ./api
        run: pnpm install --frozen-lockfile
      
      - name: Gerar Prisma Client (sem banco)
        working-directory: ./api
        env:
          DATABASE_URL: postgresql://user:pass@localhost:5432/db?schema=public
        run: pnpm run generate
      
      - name: Executar testes unitários
        working-directory: ./api
        run: pnpm run test:unit
      
      - name: Upload de coverage
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: coverage-unit
          path: ./api/coverage/
          retention-days: 7
        continue-on-error: true

  testes-e2e:
    name: Testes End-to-End
    needs: [testes-unitarios]
    runs-on: ubuntu-latest
    environment: homologacao
    timeout-minutes: 30
    
    steps:
      - name: Checkout do código
        uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
      
      - name: Ativar pnpm
        run: corepack enable
      
      - name: Cache de dependências
        uses: actions/cache@v4
        with:
          path: |
            ./api/node_modules
            ~/.pnpm-store
          key: ${{ runner.os }}-pnpm-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-
      
      - name: Instalar dependências
        working-directory: ./api
        run: pnpm install --frozen-lockfile
      
      - name: Preparar e subir infraestrutura de testes
        working-directory: ./api
        timeout-minutes: 8
        run: |
          #!/bin/bash
          set -e
          
          echo "=== PREPARAÇÃO E SUBIDA DA INFRAESTRUTURA ==="
          
          # 1. Derrubar compose existente
          echo "Derrubando docker-compose existente..."
          docker compose -f docker-compose.test.yml down -v --remove-orphans --timeout 10 2>/dev/null || true
          
          # 2. Limpeza seletiva por labels
          echo ""
          echo "Removendo recursos do HelpMe por labels..."
          
          CONTAINERS=$(docker ps -aq --filter "label=project=helpme" --filter "label=env=test" | wc -l)
          if [ "$CONTAINERS" -gt 0 ]; then
            echo "  Removendo $CONTAINERS container(s)..."
            docker ps -aq --filter "label=project=helpme" --filter "label=env=test" | xargs -r docker rm -f 2>/dev/null || true
          fi
          
          NETWORKS=$(docker network ls --filter "label=project=helpme" -q | wc -l)
          if [ "$NETWORKS" -gt 0 ]; then
            echo "  Removendo $NETWORKS rede(s)..."
            docker network ls --filter "label=project=helpme" -q | xargs -r docker network rm 2>/dev/null || true
          fi
          
          VOLUMES=$(docker volume ls --filter "label=project=helpme" -q | wc -l)
          if [ "$VOLUMES" -gt 0 ]; then
            echo "  Removendo $VOLUMES volume(s)..."
            docker volume ls --filter "label=project=helpme" -q | xargs -r docker volume rm -f 2>/dev/null || true
          fi
          
          # 3. Liberar portas específicas AGRESSIVAMENTE
          echo ""
          echo "Liberando portas do HelpMe..."
          PORTAS=(5433 27018 6379 2181 9092 8086)
          for porta in "${PORTAS[@]}"; do
            echo "  Verificando porta ${porta}..."
            # Matar processo na porta
            sudo fuser -k ${porta}/tcp 2>/dev/null || true
            # Verificar se liberou
            if sudo lsof -i :${porta} >/dev/null 2>&1; then
              echo "  [WARN] Porta ${porta} ainda ocupada, forçando..."
              sudo lsof -ti:${porta} | xargs -r sudo kill -9 2>/dev/null || true
            fi
          done
          
          # 4. Aguardar liberação
          echo ""
          echo "Aguardando liberação de recursos (10s)..."
          sleep 10
          
          # 5. Verificação RIGOROSA
          echo ""
          echo "=== VERIFICAÇÃO DE LIMPEZA ==="
          
          REMAINING=$(docker ps -aq --filter "label=project=helpme" | wc -l)
          if [ "$REMAINING" -gt 0 ]; then
            echo "[ERROR] Ainda existem $REMAINING containers do HelpMe!"
            docker ps -a --filter "label=project=helpme"
            exit 1
          fi
          
          # Verificar portas
          PORTAS_OCUPADAS=false
          for porta in "${PORTAS[@]}"; do
            if sudo lsof -i :${porta} >/dev/null 2>&1; then
              echo "[ERROR] Porta ${porta} ainda está ocupada!"
              sudo lsof -i :${porta}
              PORTAS_OCUPADAS=true
            fi
          done
          
          if [ "$PORTAS_OCUPADAS" = true ]; then
            echo "[ERROR] Algumas portas ainda estão ocupadas!"
            exit 1
          fi
          
          echo "[SUCESSO] Ambiente 100% limpo"
          
          # 6. Pull de imagens
          echo ""
          echo "=== BAIXANDO IMAGENS DOCKER ==="
          docker compose -f docker-compose.test.yml pull --quiet
          echo "[SUCESSO] Imagens baixadas"
          
          # 7. Subir containers
          echo ""
          echo "=== SUBINDO CONTAINERS ==="
          docker compose -f docker-compose.test.yml up -d
          echo "[SUCESSO] Containers iniciados"
          
          # 8. Aguardar inicialização (Kafka/Zookeeper precisam de ~60s)
          echo ""
          echo "Aguardando inicialização dos serviços (60s)..."
          sleep 60
          
          # 9. Mostrar status
          echo ""
          echo "Status dos containers:"
          docker compose -f docker-compose.test.yml ps

      - name: Gerar Prisma Client (com banco real)
        working-directory: ./api
        env:
          DATABASE_URL: postgresql://teste:senha_teste@localhost:5433/helpme_database_teste?schema=public
        run: pnpm run generate

      - name: Criar/sobrescrever .env.test com configurações do CI
        working-directory: ./api
        run: |
          echo "=== CRIANDO .env.test PARA CI ==="
          cat > .env.test << 'EOF'
          # PostgreSQL
          DATABASE_URL=postgresql://teste:senha_teste@localhost:5433/helpme_database_teste?schema=public
          
          # MongoDB (SEM autenticação)
          MONGO_URL=mongodb://localhost:27018/helpme_logs_teste
          MONGO_INITDB_DATABASE=helpme_logs_teste
          
          # Redis (SEM senha)
          REDIS_HOST=localhost
          REDIS_PORT=6379
          
          # Kafka - PORTA 9092
          KAFKA_BROKERS=localhost:9092
          KAFKA_CLIENT_ID=helpdesk-api
          
          # InfluxDB
          INFLUX_URL=http://localhost:8086
          INFLUX_TOKEN=teste_token_influxdb_12345678
          INFLUX_ORG=helpme_org
          INFLUX_BUCKET=helpme_metrics
          INFLUX_USERNAME=admin
          INFLUX_PASSWORD=admin_password_teste
          
          # JWT
          JWT_SECRET=test_secret_key_for_testing_only
          JWT_EXPIRES_IN=1h
          
          # Ambiente
          NODE_ENV=test
          PORT=3000
          EOF
          
          echo "[SUCESSO] .env.test criado"
          echo ""
          echo "Conteúdo do .env.test:"
          cat .env.test

      - name: Executar migrations
        working-directory: ./api
        env:
          DATABASE_URL: postgresql://teste:senha_teste@localhost:5433/helpme_database_teste?schema=public
        run: |
          echo "=== EXECUTANDO MIGRATIONS ==="
          pnpm run migrate
          echo "[SUCESSO] Migrations executadas"
      
      - name: Popular banco de dados (seed)
        working-directory: ./api
        run: |
          echo "=== POPULANDO BANCO DE DADOS ==="
          pnpm run seed-big-ci
          echo "[SUCESSO] Seed executado"

      - name: Executar testes E2E
        working-directory: ./api
        env:
          # PostgreSQL
          DATABASE_URL: postgresql://teste:senha_teste@localhost:5433/helpme_database_teste?schema=public
          
          # MongoDB (SEM autenticação conforme docker-compose.test.yml)
          MONGO_URL: mongodb://localhost:27018/helpme_logs_teste
          MONGO_INITDB_DATABASE: helpme_logs_teste
          
          # Redis (SEM senha conforme docker-compose.test.yml)
          REDIS_HOST: localhost
          REDIS_PORT: 6379
          
          # Kafka
          KAFKA_BROKERS: localhost:9092
          KAFKA_CLIENT_ID: helpdesk-api
          
          # InfluxDB
          INFLUX_URL: http://localhost:8086
          INFLUX_TOKEN: teste_token_influxdb_12345678
          INFLUX_ORG: helpme_org
          INFLUX_BUCKET: helpme_metrics
          INFLUX_USERNAME: admin
          INFLUX_PASSWORD: admin_password_teste
          
          # Ambiente
          NODE_ENV: test
        run: |
          echo "=== EXECUTANDO TESTES E2E ==="
          # Não usar pnpm test:e2e porque ele roda test:setup que sobe containers de novo
          # A infraestrutura já está rodando no step anterior
          pnpm exec vitest run src/__tests__/e2e
          echo "[SUCESSO] Testes E2E concluídos"

      - name: Upload de coverage E2E
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: coverage-e2e
          path: ./api/coverage/
          retention-days: 7
        continue-on-error: true
      
      - name: Logs dos serviços (em caso de falha)
        working-directory: ./api
        if: failure()
        run: |
          echo "=== LOGS DOS SERVIÇOS (ÚLTIMAS 100 LINHAS) ==="
          
          echo "PostgreSQL:"
          docker compose -f docker-compose.test.yml logs --tail=100 postgres
          
          echo ""
          echo "MongoDB:"
          docker compose -f docker-compose.test.yml logs --tail=100 mongodb
          
          echo ""
          echo "Redis:"
          docker compose -f docker-compose.test.yml logs --tail=100 redis
          
          echo ""
          echo "Zookeeper:"
          docker compose -f docker-compose.test.yml logs --tail=100 zookeeper
          
          echo ""
          echo "Kafka:"
          docker compose -f docker-compose.test.yml logs --tail=100 kafka
          
          echo ""
          echo "InfluxDB:"
          docker compose -f docker-compose.test.yml logs --tail=100 influxdb

      - name: Derrubar infraestrutura
        working-directory: ./api 
        if: always()
        run: |
          echo "=== DERRUBANDO INFRAESTRUTURA ==="
          docker compose -f docker-compose.test.yml down -v --timeout 30
          echo "[SUCESSO] Infraestrutura derrubada"

  release:
    name: Build e Push Docker Image
    runs-on: ubuntu-latest
    needs: [build, testes-unitarios, testes-e2e]
    environment: homologacao
    outputs:
      version: ${{ needs.build.outputs.version }}
      tags: ${{ steps.meta.outputs.tags }}
    
    steps:
      - name: Checkout do código
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      
      - name: Extrair metadados Docker
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ secrets.DOCKERHUB_USERNAME }}/helpme-api
          tags: |
            type=raw,value=${{ needs.build.outputs.version }}
            type=raw,value=latest
            type=raw,value=homolog
            type=sha,prefix=sha-
            type=raw,value=build-${{ github.run_number }}
          flavor: |
            latest=auto
      
      - name: Análise do Dockerfile com Hadolint
        uses: hadolint/hadolint-action@v3.1.0
        with:
          dockerfile: ./api/Dockerfile
        continue-on-error: true
      
      - name: Login no Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      
      - name: Setup Docker Buildx
        uses: docker/setup-buildx-action@v3
      
      - name: Build e Push da imagem
        uses: docker/build-push-action@v6
        with:
          push: true
          context: ./api
          file: ./api/Dockerfile
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=registry,ref=${{ secrets.DOCKERHUB_USERNAME }}/helpme-api:buildcache
          cache-to: type=registry,ref=${{ secrets.DOCKERHUB_USERNAME }}/helpme-api:buildcache,mode=max
          build-args: |
            VERSION=${{ needs.build.outputs.version }}
            BUILD_DATE=${{ fromJSON(steps.meta.outputs.json).labels['org.opencontainers.image.created'] }}
            GIT_COMMIT=${{ github.sha }}
      
      - name: Scan de vulnerabilidades com Trivy
        uses: aquasecurity/trivy-action@0.32.0
        with:
          scan-type: 'image'
          image-ref: ${{ secrets.DOCKERHUB_USERNAME }}/helpme-api:${{ needs.build.outputs.version }}
          format: sarif
          output: trivy-results.sarif
          severity: "UNKNOWN,LOW,MEDIUM,HIGH,CRITICAL"
        continue-on-error: true
      
      - name: Upload resultados do Trivy
        uses: github/codeql-action/upload-sarif@v3
        if: always()
        with:
          category: trivy-docker-scan
          sarif_file: trivy-results.sarif
        continue-on-error: true
      
      - name: Criar tag Git
        if: ${{ inputs.create-tag }}
        run: |
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          git tag -a "v${{ needs.build.outputs.version }}" -m "Release v${{ needs.build.outputs.version }}"
          git push origin "v${{ needs.build.outputs.version }}"
          echo "[SUCESSO] Tag v${{ needs.build.outputs.version }} criada e enviada"
      
      - name: Resumo da release
        run: |
          echo "## Release v${{ needs.build.outputs.version }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          
          echo "### Versão" >> $GITHUB_STEP_SUMMARY
          echo "- **Versão**: \`${{ needs.build.outputs.version }}\`" >> $GITHUB_STEP_SUMMARY
          
          if [ -n "${{ inputs.version-override }}" ]; then
            echo "- **Origem**: Manual override" >> $GITHUB_STEP_SUMMARY
          elif [ "${{ inputs.bump-type }}" != "none" ]; then
            echo "- **Origem**: Auto-incremento (${{ inputs.bump-type }})" >> $GITHUB_STEP_SUMMARY
          else
            echo "- **Origem**: package.json" >> $GITHUB_STEP_SUMMARY
          fi
          
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Imagens Docker Publicadas" >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
          echo "${{ steps.meta.outputs.tags }}" >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
          
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Informações da Build" >> $GITHUB_STEP_SUMMARY
          echo "- **Commit**: \`${{ github.sha }}\`" >> $GITHUB_STEP_SUMMARY
          echo "- **Build**: #${{ github.run_number }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Executor**: @${{ github.actor }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Data**: $(date -u +"%Y-%m-%d %H:%M:%S UTC")" >> $GITHUB_STEP_SUMMARY
          
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Como usar:" >> $GITHUB_STEP_SUMMARY
          echo '```bash' >> $GITHUB_STEP_SUMMARY
          echo "# Versão específica" >> $GITHUB_STEP_SUMMARY
          echo "docker pull ${{ secrets.DOCKERHUB_USERNAME }}/helpme-api:${{ needs.build.outputs.version }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "# Última versão" >> $GITHUB_STEP_SUMMARY
          echo "docker pull ${{ secrets.DOCKERHUB_USERNAME }}/helpme-api:latest" >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY
          
          if [ "${{ inputs.create-tag }}" == "true" ]; then
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "### Tag Git criada" >> $GITHUB_STEP_SUMMARY
            echo "Tag **v${{ needs.build.outputs.version }}** foi criada no repositório" >> $GITHUB_STEP_SUMMARY
          fi
          
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "---" >> $GITHUB_STEP_SUMMARY
          echo "Pipeline executada com sucesso!" >> $GITHUB_STEP_SUMMARY